

import torch.nn.functional as f
import torch.nn as nn
import math
import torch

a = torch.BoolTensor()
# x = torch.randn((16, 5))
# w = torch.randn((10, 5))
# x = f.normalize(x, dim=1)
# w = f.normalize(w, dim=1)
# r = f.linear(x, w)
# print(r)
# print(r.shape)


ap = 0.005
a = 'ap{}'.format(ap)
print(a)